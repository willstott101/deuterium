[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
develop = "sh -c 'python -m maturin develop ; python -i -c \"from deuterium import *\"'"
test = "python -m pytest"

[packages]
pytest = "*"

[dev-packages]
maturin = "*"

[requires]
python_version = "3.9"
